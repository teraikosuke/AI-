# 現在のAPI構成図

**作成日**: 2026-02-06

---

## 概要

```
┌─────────────────────────────────────────────────────────────────┐
│                        フロントエンド                            │
│                   (HTML/JS 静的ファイル)                        │
└───────────────────────────┬─────────────────────────────────────┘
                            │ HTTPS
                            ▼
┌─────────────────────────────────────────────────────────────────┐
│                      API Gateway (HTTP API)                     │
│                         ats-lite-api                            │
│           https://uqg1gdotaa.execute-api.ap-northeast-1.amazonaws.com │
│                                                                 │
│  ステージ: /dev  (現在使用中)                                     │
│  ステージ: $default                                              │
└───────────────────────────┬─────────────────────────────────────┘
                            │
                            ▼
┌─────────────────────────────────────────────────────────────────┐
│                    Lambda関数群 (21個)                          │
│                    ats-api-dev-*                                │
└───────────────────────────┬─────────────────────────────────────┘
                            │
                            ▼
┌─────────────────────────────────────────────────────────────────┐
│                      RDS (PostgreSQL)                           │
│               ats-lite-db.*.ap-northeast-1.rds.amazonaws.com    │
└─────────────────────────────────────────────────────────────────┘
```

---

## API Gateway 詳細

| 項目 | 値 |
|------|-----|
| **API名** | ats-lite-api |
| **タイプ** | HTTP API |
| **API ID** | uqg1gdotaa |
| **エンドポイント** | https://uqg1gdotaa.execute-api.ap-northeast-1.amazonaws.com |
| **ステージ** | dev, $default |

---

## エンドポイント一覧

### 🔐 認証系
| メソッド | パス | Lambda関数 |
|----------|------|------------|
| POST | /auth/login | ats-api-dev-auth-login |
| GET | /auth/me | ats-api-dev-auth-me |

### 👥 候補者管理
| メソッド | パス | Lambda関数 |
|----------|------|------------|
| GET | /candidates | ats-api-dev-candidates-list |
| GET | /candidates/{id} | ats-api-dev-candidates-detail |
| PUT | /candidates/{id} | ats-api-dev-candidates-detail |

### 📊 KPI・実績
| メソッド | パス | Lambda関数 |
|----------|------|------------|
| GET | /kpi/yield | ats-api-dev-kpi-yield |
| GET | /kpi/yield/daily | ats-api-dev-kpi-yield-daily |
| GET | /kpi/yield/personal | ats-api-dev-kpi-yield-personal |
| GET | /kpi/yield/company | ats-api-dev-kpi-yield-company |
| GET | /kpi/clients | ats-api-dev-kpi-clients |
| GET | /kpi/ads | ats-api-dev-kpi-ads |
| GET | /ads/detail | ats-api-dev-kpi-ads-detail |
| GET | /kpi/teleapo | ats-api-dev-kpi-teleapo |
| GET/PUT | /kpi-targets | ats-api-dev-kpi-targets |

### 📞 テレアポ
| メソッド | パス | Lambda関数 |
|----------|------|------------|
| GET | /teleapo/logs | ats-api-dev-teleapo-logs |
| PUT/DELETE | /teleapo/logs | ats-api-dev-teleapo-log-create |
| GET | /teleapo/candidate-contact | ats-api-dev-teleapo-candidate-contact |

### 🎯 目標設定
| メソッド | パス | Lambda関数 |
|----------|------|------------|
| GET/PUT | /goal/goal-settings | ats-api-dev-goal-settings |
| GET/PUT | /goal/goal-targets | ats-api-dev-goal-settings |
| GET/PUT | /goal/goal-daily-targets | ats-api-dev-goal-settings |
| GET/PUT | /important-metrics | ats-api-dev-goal-settings |
| GET/PUT | /ms-targets | ats-api-dev-ms-targets |

### 👨‍💼 メンバー管理
| メソッド | パス | Lambda関数 |
|----------|------|------------|
| GET/POST/PUT/DELETE | /members | ats-api-dev-members |
| GET | /members/requests | ats-api-dev-members |
| POST | /members/approve | ats-api-dev-members |
| GET | /members/verify | ats-api-dev-members |

### 🏢 クライアント
| メソッド | パス | Lambda関数 |
|----------|------|------------|
| GET/PUT | /clients | ats-api-dev-kpi-clients-edit |
| POST | /clients | ats-api-dev-clients-create |
| POST/PUT | /kpi/clients | ats-api-dev-kpi-clients |

### ⚙️ 設定
| メソッド | パス | Lambda関数 |
|----------|------|------------|
| GET/PUT | /settings-screening-rules | ats-api-settings-screening-rules |

### 🏠 マイページ
| メソッド | パス | Lambda関数 |
|----------|------|------------|
| GET | /mypage | ats-api-dev-mypage |

---

## 本番環境構成（予定）

```
┌─────────────────────────────────────────────────────────────────┐
│                        フロントエンド                            │
│               (Cloudflare Pages or AWS Amplify)                 │
└───────────────────────────┬─────────────────────────────────────┘
                            │ HTTPS
                            ▼
┌─────────────────────────────────────────────────────────────────┐
│                    API Gateway (HTTP API)                       │
│                     ats-lite-api-prod (新規作成)                 │
│                                                                 │
│  エンドポイント: https://xxx.execute-api.ap-northeast-1.amazonaws.com │
└───────────────────────────┬─────────────────────────────────────┘
                            │
                            ▼
┌─────────────────────────────────────────────────────────────────┐
│                    Lambda関数群 (22個) ✅ 作成済み               │
│                    ats-api-prod-*                               │
└───────────────────────────┬─────────────────────────────────────┘
                            │
                            ▼
┌─────────────────────────────────────────────────────────────────┐
│                      RDS (PostgreSQL)                           │
│               (現在は開発DBと共有、後で分離可能)                   │
└─────────────────────────────────────────────────────────────────┘
```

---

## 次のステップ

1. ✅ 本番用Lambda関数（22個）作成完了
2. ❓ 本番用API Gateway作成
3. ❓ フロントエンドホスティング設定
4. ❓ (オプション) 本番用DB分離
