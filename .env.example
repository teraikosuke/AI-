# ==============================================================================
# Agent Key - 環境変数設定例
# ==============================================================================
# このファイルをコピーして .env を作成してください
# cp .env.example .env
#
# ⚠️ 重要: .env ファイルは絶対にGitにコミットしないでください！
# ==============================================================================

# ------------------------------------------------------------------------------
# データベース接続 (PostgreSQL)
# ------------------------------------------------------------------------------
DATABASE_URL="postgres://USER:PASSWORD@HOST:PORT/DATABASE"

# Lambda環境変数として設定する場合は以下を使用
# DB_HOST="your-rds-endpoint.ap-northeast-1.rds.amazonaws.com"
# DB_PORT="5432"
# DB_NAME="app"
# DB_USER="app"
# DB_PASSWORD="your-secure-password"

# ------------------------------------------------------------------------------
# Kintone連携 (外部データ同期用)
# ------------------------------------------------------------------------------
# Kintoneサブドメイン (例: your-company)
KINTONE_SUBDOMAIN="your-subdomain"

# KintoneアプリID
KINTONE_APP_ID="your-app-id"

# Kintone APIトークン
# ⚠️ 本番環境ではAWS Secrets Managerに保存することを推奨
KINTONE_API_TOKEN="your-api-token"

# ------------------------------------------------------------------------------
# JWT認証設定
# ------------------------------------------------------------------------------
# JWT署名用シークレット (最低32文字の強力なランダム文字列を使用)
# 生成方法: openssl rand -base64 32
JWT_SECRET="generate-a-strong-secret-key-here"

# JWTトークンの有効期限 (時間)
JWT_TTL_HOURS="12"

# ------------------------------------------------------------------------------
# CORS設定
# ------------------------------------------------------------------------------
# 許可するオリジン (カンマ区切りで複数指定可能)
# 開発環境:
CORS_ALLOWED_ORIGINS="http://localhost:8000,http://localhost:8001,http://localhost:8081"

# 本番環境:
# CORS_ALLOWED_ORIGINS="https://app.agent-key.example.com"

# ------------------------------------------------------------------------------
# 環境設定
# ------------------------------------------------------------------------------
# development | staging | production
NODE_ENV="development"
